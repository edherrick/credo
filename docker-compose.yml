services:
  db:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_DB: credo
      POSTGRES_USER: credo
      POSTGRES_PASSWORD: credo
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
